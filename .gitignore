# Dépendances
node_modules/
npm-debug.log*
package-lock.json   # varie selon l'environnement (Ubuntu vs sandbox) — cause des conflits git pull récurrents

# Données locales & sorties (jamais versionnées)
data/
output/
dist/
*.log

# Secrets — ne doivent JAMAIS être commités
.env
.env.*
!.env.example
*.pem
*.key

# Système
.DS_Store
.vscode/
.idea/

# Cookies de session (jamais commit)
cookies/*.txt
